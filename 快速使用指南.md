# Matrix 符号化服务 - 快速使用指南

## 🚀 启动服务器

```bash
cd /Users/momo/Desktop/MatrixTestApp/matrix-symbolicate-server
go run main.go symbolicate.go format.go
```

**访问地址：** http://localhost:8080

---

## 📦 上传符号表

### 方法 1：命令行上传（推荐）

```bash
cd /Users/momo/Desktop/MatrixTestApp
./upload_dsym.sh
```

### 方法 2：网页上传

1. 打开 http://localhost:8080
2. 点击「上传符号表」
3. 选择 `.dSYM.zip` 文件

---

## 📱 iOS 配置

编辑 `MatrixTestApp/Info.plist` 设置服务器地址：

```xml
<key>MatrixServerURL</key>
<string>http://localhost:8080</string>
```

**真机测试时改为：** `http://YOUR_MAC_IP:8080`

---

## 🔍 查看报告

浏览器打开：http://localhost:8080

- 查看已上传的符号表
- 查看自动上报的卡顿日志
- 点击「可读格式」查看符号化后的报告

---

## 🛠️ 常用命令

**停止服务器：**
```bash
lsof -ti:8080 | xargs kill -9
```

**重启服务器：**
```bash
lsof -ti:8080 | xargs kill -9
cd /Users/momo/Desktop/MatrixTestApp/matrix-symbolicate-server
go run main.go symbolicate.go format.go
```

**检查服务器状态：**
```bash
curl http://localhost:8080/api/health
```

---

## 📝 使用流程

1. **启动服务器** → 运行 Go 服务
2. **上传符号表** → 每次重新编译后上传新的 dSYM
3. **运行 App** → iOS 应用自动上报卡顿日志
4. **查看分析** → 网页查看符号化后的可读报告

---

**💡 提示：** 符号表需要与 App 版本对应，每次编译后记得上传最新的符号表！

关闭服务器很简单，运行以下命令：

[2 tools called]

✅ **服务器已成功关闭！**

---

## 📝 服务器操作命令总结

### 🚀 **启动服务器**
```bash
cd /Users/momo/Desktop/MatrixTestApp/matrix-symbolicate-server
./matrix-server
```

或者使用脚本：
```bash
./start.sh
```

### 🛑 **关闭服务器**
```bash
lsof -ti:8080 | xargs kill
```

### 🔍 **检查服务器状态**
```bash
curl http://localhost:8080/api/health
```

### 📊 **查看服务器日志**
如果在前台运行，直接看终端输出。如果在后台运行，查看：
```bash
tail -f server.log
```

---

现在服务器已经完全关闭了！需要重新启动时，直接运行 `./matrix-server` 即可。
